---
title: "@tooly/linear"
description: Project management tools powered by Linear API for AI applications
---

The `@tooly/linear` package provides AI-ready project management functionality powered by the Linear API. Built specifically for the Vercel AI SDK, it's perfect for issue tracking, project management, and team collaboration in AI applications.

## Installation

```bash
npm install @tooly/linear
```

## Quick Start with Vercel AI SDK

```typescript
import { generateText } from "ai";
import { openai } from "@ai-sdk/openai";
import { createVercelAITools } from "@tooly/linear";

const tools = createVercelAITools("your-linear-api-key");

// Use with Vercel AI SDK
const result = await generateText({
  model: openai("gpt-4"),
  messages: [
    {
      role: "user",
      content: "Create a bug report for the login page not working properly"
    }
  ],
  tools,
});
```

## API Key Setup

Get your Linear API key from [Linear Settings](https://linear.app/settings/api):

```typescript
// Using environment variables (recommended)
const tools = createVercelAITools(process.env.LINEAR_API_KEY!);

// Or directly (not recommended for production)
const tools = createVercelAITools("lin_api_your_key_here");
```

## Available Tools

### createIssue

Create a new issue in Linear with full details and metadata.

**Parameters:**
- `title` (string): Issue title
- `description` (string, optional): Issue description in markdown
- `teamId` (string): Team ID where the issue should be created
- `assigneeId` (string, optional): User ID to assign the issue to
- `priority` (number, optional): Priority level (1-4, where 1 is urgent)
- `labelIds` (string[], optional): Array of label IDs to apply

**Example:**
```typescript
const result = await generateText({
  model: openai("gpt-4"),
  messages: [
    {
      role: "user",
      content: "Create a high-priority bug report for the payment system failing"
    }
  ],
  tools,
});
```

### getIssue

Retrieve detailed information about a specific issue.

**Parameters:**
- `issueId` (string): The Linear issue ID

### updateIssue

Update an existing issue with new information or status changes.

**Parameters:**
- `issueId` (string): The Linear issue ID
- `title` (string, optional): Updated title
- `description` (string, optional): Updated description
- `assigneeId` (string, optional): Updated assignee
- `priority` (number, optional): Updated priority
- `stateId` (string, optional): Updated workflow state

### searchIssues

Search for issues using various criteria and filters.

**Parameters:**
- `query` (string, optional): Search query text
- `teamId` (string, optional): Filter by specific team
- `assigneeId` (string, optional): Filter by assignee
- `stateId` (string, optional): Filter by workflow state
- `priority` (number, optional): Filter by priority level
- `limit` (number, optional): Maximum number of results (default: 10)

### createProject

Create a new project to organize related issues and milestones.

**Parameters:**
- `name` (string): Project name
- `description` (string, optional): Project description
- `teamIds` (string[]): Array of team IDs that will work on this project
- `leadId` (string, optional): User ID of the project lead
- `targetDate` (string, optional): Target completion date (ISO format)

### getTeams

Retrieve all teams in your Linear workspace.

### getUser

Get information about a specific user by their email address.

**Parameters:**
- `email` (string): User's email address

## Vercel AI SDK Integration

### Basic Issue Creation

```typescript
import { generateText } from "ai";
import { openai } from "@ai-sdk/openai";
import { createVercelAITools } from "@tooly/linear";

const tools = createVercelAITools(process.env.LINEAR_API_KEY!);

const result = await generateText({
  model: openai("gpt-4.1-nano"),
  messages: [
    {
      role: "user",
      content: "Create a bug report for the checkout page crashing when users apply discount codes"
    }
  ],
  tools,
});
```

### Streaming with Tool Calls

```typescript
import { streamText } from "ai";
import { openai } from "@ai-sdk/openai";
import { createVercelAITools } from "@tooly/linear";

const tools = createVercelAITools(process.env.LINEAR_API_KEY!);

const { textStream, toolCallStream } = await streamText({
  model: openai("gpt-4.1-nano"),
  messages: [
    {
      role: "user",
      content: "Search for all critical bugs and create a summary report"
    }
  ],
  tools,
});

// Stream the text response
for await (const textPart of textStream) {
  process.stdout.write(textPart);
}

// Handle tool calls
for await (const toolCall of toolCallStream) {
  console.log("Tool call:", toolCall);
}
```

### Multiple AI Providers

```typescript
import { generateText } from "ai";
import { anthropic } from "@ai-sdk/anthropic";
import { createVercelAITools } from "@tooly/linear";

const tools = createVercelAITools(process.env.LINEAR_API_KEY!);

// Works with any AI provider
const result = await generateText({
  model: anthropic("claude-sonnet-4-20250514"),
  messages: [
    {
      role: "user",
      content: "Create a project for our upcoming mobile app redesign with Q2 deadline"
    }
  ],
  tools,
});
```

### Project Management Workflows

```typescript
import { generateText } from "ai";
import { openai } from "@ai-sdk/openai";
import { createVercelAITools } from "@tooly/linear";

const tools = createVercelAITools(process.env.LINEAR_API_KEY!);

// AI can handle complex project management tasks
const result = await generateText({
  model: openai("gpt-4.1-nano"),
  messages: [
    {
      role: "system",
      content: "You are a project management assistant. Help organize and track development work."
    },
    {
      role: "user",
      content: `Based on this customer feedback: "The mobile app is slow and crashes frequently", 
                create appropriate issues and assign them to the mobile team.`
    }
  ],
  tools,
});
```

## Alternative: OpenAI Function Calling

```typescript
import OpenAI from "openai";
import { createOpenAIFunctions } from "@tooly/linear";

const openai = new OpenAI();
const { tools, executeFunction } = createOpenAIFunctions(process.env.LINEAR_API_KEY!);

const completion = await openai.chat.completions.create({
  model: "gpt-4.1-nano",
  messages: [
    {
      role: "user",
      content: "Search for all open issues assigned to john@company.com"
    }
  ],
  tools,
});

// Handle tool calls
for (const toolCall of completion.choices[0].message.tool_calls || []) {
  const result = await executeFunction(
    toolCall.function.name,
    JSON.parse(toolCall.function.arguments)
  );
  console.log("Linear operation result:", result);
}
```

## Alternative: Anthropic Claude

```typescript
import Anthropic from "@anthropic-ai/sdk";
import { createAnthropicTools } from "@tooly/linear";

const anthropic = new Anthropic();
const { tools, executeFunction } = createAnthropicTools(process.env.LINEAR_API_KEY!);

const message = await anthropic.messages.create({
  model: "claude-sonnet-4-20250514",
  messages: [
    {
      role: "user",
      content: "Create a project for our upcoming mobile app redesign"
    }
  ],
  tools,
});

// Handle tool use
for (const toolUse of message.content.filter((c) => c.type === "tool_use")) {
  const result = await executeFunction(toolUse.name, toolUse.input);
  console.log("Project created:", result);
}
```

## Direct Usage

You can also use the package directly without AI frameworks:

```typescript
import { LinearTools } from "@tooly/linear";

const linear = new LinearTools("your-api-key");

// Create an issue directly
const issue = await linear.executeFunction("createIssue", {
  title: "Performance optimization needed",
  description: "The dashboard loads slowly with large datasets",
  teamId: "team-123",
  priority: 3,
});

console.log("Issue created:", issue);
```

## Error Handling

The package includes comprehensive error handling:

```typescript
try {
  const result = await generateText({
    model: openai("gpt-4"),
    messages: [
      {
        role: "user",
        content: "Create an issue with invalid data"
      }
    ],
    tools,
  });
} catch (error) {
  if (error.name === "ZodError") {
    console.log("Validation errors:", error.errors);
  } else if (error.message.includes("Team not found")) {
    console.log("Linear API error:", error.message);
  } else {
    console.log("Unexpected error:", error);
  }
}
```

## Working with Teams and Users

### Getting Team Information

```typescript
// AI can help you find and work with teams
const result = await generateText({
  model: openai("gpt-4.1-nano"),
  messages: [
    {
      role: "user",
      content: "Get all teams and create a bug report for the engineering team"
    }
  ],
  tools,
});
```

### User Management

```typescript
// AI can find users and assign work
const result = await generateText({
  model: openai("gpt-4.1-nano"),
  messages: [
    {
      role: "user",
      content: "Find the user john@company.com and assign them a code review task"
    }
  ],
  tools,
});
```

## Priority and State Management

### Priority Levels

Linear uses numeric priority levels:
- `1`: Urgent (🔴)
- `2`: High (🟠)
- `3`: Medium (🟡)
- `4`: Low (🔵)

```typescript
// AI can intelligently set priorities based on context
const result = await generateText({
  model: openai("gpt-4.1-nano"),
  messages: [
    {
      role: "user",
      content: "Create a critical security vulnerability report for the authentication system"
    }
  ],
  tools,
});
```

## Best Practices

### 1. Environment Variables
Always store API keys securely:

```bash
LINEAR_API_KEY=lin_api_your_key_here
```

### 2. Team ID Management
Let AI handle team discovery:

```typescript
// Instead of hardcoding team IDs, let AI find them
const result = await generateText({
  model: openai("gpt-4.1-nano"),
  messages: [
    {
      role: "user",
      content: "Create a backend issue for the API team"
    }
  ],
  tools,
});
```

### 3. Issue Templates
Create reusable issue templates that AI can use:

```typescript
// AI can intelligently use templates based on context
const result = await generateText({
  model: openai("gpt-4.1-nano"),
  messages: [
    {
      role: "system",
      content: `When creating bug reports, use this template:
      - Clear, descriptive title with 🐛 emoji
      - Steps to reproduce
      - Expected vs actual behavior
      - Environment details`
    },
    {
      role: "user",
      content: "Create a bug report for the login timeout issue"
    }
  ],
  tools,
});
```

### 4. Batch Operations
Use AI to handle multiple operations efficiently:

```typescript
const result = await generateText({
  model: openai("gpt-4.1-nano"),
  messages: [
    {
      role: "user",
      content: `Process this sprint planning request:
      - Create issues for user authentication
      - Set up project structure
      - Assign tasks to appropriate team members
      - Set priority levels based on dependencies`
    }
  ],
  tools,
});
```

## Common Use Cases

### 1. Automated Bug Reporting
```typescript
// AI can automatically create structured bug reports from user feedback
"Create a bug report based on this user feedback: 'The app crashes when I try to upload large files'"
```

### 2. Sprint Planning
```typescript
// AI can help with sprint planning and issue creation
"Plan our next sprint by creating issues for these requirements: user authentication, file upload, dashboard improvements"
```

### 3. Issue Triage
```typescript
// AI can search and organize existing issues
"Find all high-priority issues assigned to the frontend team and create a summary"
```

### 4. Project Management
```typescript
// AI can create and manage projects
"Create a project for our Q2 mobile app redesign with appropriate timeline and team assignments"
```

## Troubleshooting

### Common Issues

**1. Invalid API Key**
```
Error: Unauthorized
```
Verify your API key from Linear Settings.

**2. Team Not Found**
```
Error: Team not found
```
Let AI use `getTeams()` to find valid team IDs.

**3. User Not Found**
```
Error: User not found
```
Use AI to find users by email with `getUser()`.

**4. Rate Limit Exceeded**
```
Error: Too many requests
```
Implement delays between requests or reduce concurrent operations.

**5. Invalid Priority**
```
ZodError: Priority must be between 1 and 4
```
Use only valid priority numbers (1-4).

## Integration Examples

### Customer Support Bot
```typescript
// AI can create issues directly from customer support conversations
const result = await generateText({
  model: openai("gpt-4"),
  messages: [
    {
      role: "user",
      content: "Customer John Smith reported: 'Dashboard won't load for our Enterprise account since yesterday. Multiple users affected.'"
    }
  ],
  tools,
});
```

### Development Workflow
```typescript
// AI can create issues from code review feedback
const result = await generateText({
  model: openai("gpt-4"),
  messages: [
    {
      role: "user",
      content: "Code review feedback: 'The authentication logic needs refactoring for better security. Also missing error handling.'"
    }
  ],
  tools,
});
```

## Next Steps

- [📧 @tooly/resend](/docs/packages/resend) - Email functionality
- [🔧 @tooly/core](/docs/packages/core) - Build your own packages
- [💡 Examples](/docs/examples) - Real-world usage examples 